# AGENTS.md — js2java 内容生成规范

> 本文件是 AI Agent 生成本仓库学习材料时**必须遵守**的约束文档。
> 任何章节内容的生成、修改，都不得偏离此处定义的背景、目标、结构与模板。

---

## 1. 项目背景

`js2java` 是一套「前端工程师 Java 速通」学习仓库。核心方法论：**以 JavaScript/TypeScript 为锚点，通过概念对比与迁移理解，快速建立 Java 知识体系**，最终目标是前端开发者转型全栈。

学习路线总纲见 [ROADMAP.md](./ROADMAP.md)，生成任何章节前必须先阅读大纲，确保内容范围、难度、重要度与大纲一致。

## 2. 目标读者画像

拥有**多年前端开发经验**的开发者，已熟练掌握：

- JavaScript（ES6+）、TypeScript
- HTML、CSS
- Vue 生态、Vite、Webpack 等工程化工具
- Node.js 基础使用

**读者的认知特点（写作时必须利用）：**

- 熟悉动态类型、原型链、事件循环、Promise 异步模型
- 熟悉 npm 包管理、模块化（ES Module）、TS 类型系统与装饰器
- 对静态类型编译、多线程、JVM、IoC 等后端概念**零认知或仅有模糊印象**
- 讨厌从 "Hello World" 级别的内容重学一遍

## 3. 教学方式（四条铁律）

1. **概念迁移**：用前端已掌握的概念迁移理解 Java。每个知识点先回答"JS/TS 里对应什么"。
2. **相似对比**：对 Java 与 JavaScript/TypeScript 中相似或易混淆的概念进行表格对比。
3. **重点标记**：对 Java 独有、后端高频、容易误解、面试常考的内容重点标记（见第 5 节标记规范）。
4. **四问讲透**：每个核心概念必须解释——**是什么、为什么需要、解决什么问题、实际开发怎么用**。

## 4. 内容原则

- 突出 JS ↔ Java 的迁移理解，这是本仓库存在的意义，每章都必须贯彻
- 讲差异比讲语法重要；讲"为什么"比讲"怎么用"重要
- 不堆砌大而全的 API 清单，只讲后端开发真正高频的内容
- 示例代码必须可运行、有注释，避免伪代码
- 语言：简体中文；术语首次出现时标注英文原词

## 5. 重点标记规范

正文中使用以下标记标注特殊内容：

| 标记 | 含义 | 使用场景 |
|------|------|----------|
| 🆕 | Java 独有 | JS/TS 中完全没有对应概念（如受检异常、接口） |
| 🔥 | 后端高频 | 日常后端开发中天天用到的内容 |
| ⚠️ | 容易误解 | 前端开发者按 JS 直觉理解会踩坑的点（如 `==` 比较） |
| 💼 | 面试常考 | 后端面试高频考点（如 HashMap 原理、线程池参数） |

## 6. 章节结构

学习材料统一放在 `docs/` 目录，章节划分与文件命名**严格如下**，不得随意新增、改名或调整顺序：

```
docs/                          # 📘 学习文档（核心：JS vs Java 对比）
├── 01-preparation/            # 对应大纲「一、准备工作」
│   ├── jdk-jre-jvm.md         # JDK、JRE、JVM 与环境搭建
│   └── idea-debug-maven.md    # IDEA 使用、Debug 与 Maven 项目
├── 02-core-java/              # 对应大纲「二、Java」（重点）
│   ├── 01-oop.md              # 类/继承/接口/枚举（含 JS prototype 对比）
│   ├── 02-data-types.md       # 基本类型/包装类/String（含 JS 弱类型对比）
│   ├── 03-collections.md      # 流程控制 + List/Set/Map（含 JS Array/Object 对比）
│   ├── 04-io.md               # IO + 时间日期
│   ├── 05-exception.md        # 异常处理（受检异常为 Java 独有）
│   ├── 06-concurrency.md      # 线程/锁/线程池（含 JS Event Loop 对比）
│   └── 07-reflection-annotation-generic.md  # 反射、注解、泛型
├── 03-database/               # 对应大纲「三、Docker」「四、数据库」+ Redis
│   ├── 01-docker.md           # Docker 安装、容器、镜像与 Dockerfile
│   ├── 02-mysql.md            # MySQL（含 Navicat）
│   ├── 03-postgresql.md       # PostgreSQL 与 MySQL 差异
│   └── 04-redis.md            # Redis 缓存、数据结构、过期策略
└── 04-frameworks/             # 对应大纲「五、框架」
    ├── 01-maven.md            # Maven
    ├── 02-spring-boot.md      # Spring Boot
    └── 03-mybatis.md          # MyBatis

exercises/                     # 🧩 练习代码（双语言对照，同一题 JS/Java 各实现一遍）
├── javascript/                # JS 实现（用于对照）
│   ├── basics/                # 基础语法对照
│   └── algorithms/            # 算法题对照
└── java/                      # Java 实现（与 javascript/ 目录一一对应、同名同题）
    ├── basics/
    └── algorithms/

projects/                      # 🏗️ 完整项目（对应大纲「六、项目实战」「七、部署」）
├── library-system/            # 图书馆管理系统：完整 CRUD 入门项目
└── admin-system/              # Spring Boot + Vue 后台管理（对应 RuoYi 学习，含部署脚本）
```

**章节合并说明**：docs 章节是对大纲知识点的适度合并（如 OOP 五个知识点合为 `01-oop.md` 一篇），每章内部仍须按大纲知识点逐个覆盖；Redis 按用户规划归入 `03-database/`。新增或拆分章节前必须先更新本文件与 README 进度表。

每章的难度、重要程度、JS 对比锚点、完成标准以 [ROADMAP.md](./ROADMAP.md) 中的表格为准。

## 7. 章节输出模板（必须严格遵循）

```markdown
# {文档标题}

## 简介
本章学习内容：
前置知识：
阅读时长：
难度：（1-5星）
重要程度：（1-5星）
当前进度：（0%-100%） // 基于大纲进度

## 概念说明
解释本章节涉及的核心概念。

## 对比理解
使用表格对比 Java 与 JavaScript/TypeScript 中相似或容易混淆的概念。

#### 前端开发者迁移理解

## JAVA重点说明
标记说明 Java 独有或非常重要的内容：

#### 和后端开发中的实际应用

## 示例代码
提供 Java 示例代码，并在必要时提供 JavaScript/TypeScript 对比代码。

## 练习
给出本章节JAVA练习题，1-2道题

## 本章总结
进行简短的总结
```

## 8. 输出要求（生成时逐条自检）

1. **迁移理解优先**：文章必须突出 JS 和 Java 相关内容的迁移理解，「对比理解」一节是每章的核心，篇幅不得过少。
2. **星级评分**：难度与重要程度基于 ROADMAP 大纲给予 🌟 评分（1-5 星，如 🌟🌟🌟）。
3. **阅读时长**：根据章节内容量合理估算（如「约 15 分钟」），不宜虚高或虚低。
4. **当前进度**：基于大纲目录顺序合理评估（该章学完后占整个大纲的累计百分比）。
5. **练习题**：每章 1-2 道，**默认不给参考答案**，只给题目与必要提示。
6. **代码块**：Java 代码标注 ` ```java `，对比代码标注 ` ```javascript ` 或 ` ```typescript `，关键行必须有注释。
7. 生成新章节后，需在对应目录 `docs/` 下落盘，并同步更新 README 或索引中的进度（如已有索引）。
