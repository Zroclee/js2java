/**
 * 第 10 章练习：数组与集合
 * 对照文件：collections-demo.js
 *
 * 练习要求：
 * 1. 用 HashMap 统计 "hello world hello java world" 中每个单词出现次数（getOrDefault）
 * 2. List<Integer> 装 1~10，用 Iterator 和 removeIf 两种方式删除所有偶数；
 *    再在增强 for 里直接 remove，观察异常名
 * 3. 给含重复元素的 List 分别用 HashSet / LinkedHashSet / TreeSet 去重，对比顺序
 * 4. 用 ArrayDeque 实现括号匹配 isValid(String s)："({[]})" → true，"({[})]" → false
 * 5. 进阶：把 List<User> 按年龄分组为 Map<Integer, List<User>>（computeIfAbsent）
 */
public class CollectionsDemo {

    public static void main(String[] args) {
        // 在这里写你的练习代码
    }
}
