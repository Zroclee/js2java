/**
 * 第 14 章练习：反射、注解与泛型（JS 对照）
 * 对照文件：MiniIocDemo.java
 *
 * 练习要求：
 * 1. JS 的"反射"是日常：Object.keys(obj)、obj[key]、obj[methodName]()——
 *    试着用字符串名动态调用一个对象的方法，体会这在 JS 里多自然、在 Java 里多正式
 * 2. 用 JS 实现一个极简 DI 容器：register(name, instance) + get(name)，
 *    和 Java 版对比——JS 靠动态类型天然简单，Java 靠注解 + 反射 + 泛型搭出类型安全
 * 3. 如果用 TS 写过装饰器，回忆一下：TS 装饰器和 Java 注解的本质区别是什么？
 */

// 在这里写你的 JS 实现
